/*
Constructor Method:

Explanation: A constructor is a special method in a class that is called when an object of the class is created. It is used to initialize the object's state and allocate resources. Constructors have the same name as the class and do not have a return type.
*/
public class MyClass {
    private int myValue;

    // Constructor method
    public MyClass(int initialValue) {
        myValue = initialValue;
    }

    // Other methods and fields
}
